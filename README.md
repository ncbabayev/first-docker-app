# First Docker App

A simple Python application containerized with Docker.

## What it does
Prints the current date and time inside a Docker container.

## Technologies
- Python 3.11
- Docker

## How to run
```bash
docker build -t first-docker-app .
docker run first-docker-app
```